interface ServerPlayerStatsProps {
    serverId: number;
    hoursSelected: number;
    currentPlayers: number;
}
export default function ServerPlayerStats({ serverId, hoursSelected, currentPlayers }: ServerPlayerStatsProps): import("react").JSX.Element;
export {};
